{
  "name": "brickmerge-tools-userscript",
  "version": "5.5.16",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node build-userscript.mjs --sync",
    "build": "node build-userscript.mjs",
    "check": "node --check brickmerge-tweaks.js && node --check brickmerge-tweaks.runtime.js && node --check brickmerge-meta-gpt.user.js && node --check brickmerge-meta-gpt.runtime.js && node --test test/*.test.mjs",
    "test": "npm run build && npm run check"
  }
}
